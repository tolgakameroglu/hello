git clone "link"

git commit -m "commit message"

git add lists.html   

git add --all

git commit -am "merge message"

git pull

git push

git status
git branch

git checkout -b "new branch name"
git checkout master(or other branch name)

git push origin branchname:branchname (çalışılan branche push ediyorsun)
(sonra github a gidip create Pr than Merge master )

git pull origin master ( or brachname )

git rev-parse --is-inside-work-tree
(folder ın git e bağlı olup olmadığını kontrol için)


CHANGE PYTHON3 DEFAULT ALIAS ON MAC
nano ~/.bash_profile on terminal
alias python="python3" 
alias pip="pip3"

