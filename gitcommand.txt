git clone "link"

git commit -m "commit message"

git add lists.html  or git commit -am "add and commit message"

git pull

git push

git status
git branch

git checkout -b "new branch name"
git checkout master(or other branch name)

git push origin branchname:branchname (çalışılan branche push ediyorsun)
(sonra github a gidip create Pr than Merge master )




